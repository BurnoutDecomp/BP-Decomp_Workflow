# Audio faithfulness audit — 2026-08-25

Four-agent sweep of the whole audio surface (~230 TUs): GameShared Sound/Playback, GameShared Sound
root+Logic, GameSource/Sound (+SharedClasses/Sound, GameBridgeSoundToX), vendor rw/audio +
NFSMix/Nicotine/Csis + PC leaves. Verdict classes: FAITHFUL / HONEST-STUB / UNFAITHFUL
(offset-hack | fabricated-type | invented-logic | ODR-hazard) / TRIVIAL.

**Headline:** the vendor rw/audio tree, the AEMS cluster, the Splicer bodies, the Brn effect/control
layer, and the ResourceRegistrar/state-manager spine are largely FAITHFUL or honestly stubbed.
The unfaithful debt is concentrated in ~25 TUs; the items below are ranked. [BUILD] = mounted in
build_game_exe.bat (runtime impact).

## TIER 1 — build-mounted defects (fix first)

1. **[BUILD] GameShared/.../Sound/Logic/CgsVoice.cpp** — guest 32-bit offsets poked into host
   `Playback::Voice` (`+0x04` refcount, `+0x10/+0x11` state bytes, `+0x0C` ident; lines ~52-178).
   Line 121 is a live wrong-address write. STALE premise: `Playback/CgsVoice.h:181` NOW models all
   these fields by name (`mIdent`, `mu8PlaybackState`, `mu8RemoveState`, `Object::Acquire`).
2. **[BUILD] sndplayer1.cpp + sndplayer1shared.cpp** — LIVE ODR violation: two conflicting TU-local
   defs of `rw::audio::core::SndPlayer1_CgsStreamMod` (one `u8 mPad[80]+s32`, one member-less) in
   the same link. shared's `AdvanceCurrentRequest` @0x8268CD20 is a pure byte-offset transliteration
   (`lpBase+344/348/380/385/386`, stride 32) with handle/sampleRate typed `f32*` (Hex-Rays float
   artifact). Needs real CgsStreamMod layout recovery.
3. **[BUILD] GameSource/Sound/Module/LogicModule/BrnStateManager.cpp:126-131** —
   `static_cast<IResourceRequester*>(mpLogicModule)` on a `void*` misses the X360 `+0x4C90`
   sub-object walk (`addi r3,r11,0x4C90`) → virtual dispatch through the wrong vptr. Mirrored
   (unmounted) in BrnEffectObject.cpp:53-56.
4. **[BUILD] GameSource/Sound/BrnDebugComponent.cpp:26,28** — guest X360 addresses
   (0x820CDEF0/F8) stored into host `mpVtable` pointer fields. Latent wild jump.
5. **[BUILD] GameShared/.../Sound/Logic/CgsVoiceHierarchy.cpp:52-67** — fabricated 8-byte
   `rw::Resource` rival of the real 32-byte `rwcore_structs.h:73` type; `VoiceHierarchyNode::FixUp`
   @0x826A5C78 rebases from the wrong word. Also TU-local VoiceHierarchyNode (91-118) w/o header.
6. **[BUILD] RWAC/CgsSnrResourceType.cpp:23** — `FixUp` @0x826C2258 rebases a serialized 32-bit
   pointer field through `uintptr_t*` (8-byte RMW over a 4-byte field → corrupts adjacent word).
7. **[BUILD] Splicer/CgsSplicerFactory.cpp:53-59** — fabricated base-less `SplicerFactory` local
   struct vs real `CgsSplicerFactory.h:30 : public Factory`; documented in sibling dtor TU, unfixed.
8. **[BUILD] GameSource/Sound/Collision/BrnCollisionStateManager.cpp:369-381** — `SelectBin`
   @0x826A0598: placeholder zero hash table (`dword_83005F24` unrecovered) makes every name miss →
   always returns fallback bin 1. Table is rodata-recoverable from the decrypted XEX.
9. **[BUILD] GameSource/Sound/Module/LogicModule/BrnEmitterStateManager.cpp:149** — passes 0 for
   baseTypeInfo; all 7 siblings pass `StateManager::GetStaticTypeInfo()`. Breaks RTTI base chain id 7.
10. **[BUILD] GameShared/.../Sound/Logic/CgsMicrophone.h:39-75** — fabricated global-namespace
    `::Utils::DataPoint<T>` (mCurrent/mPrevious, Set/Reset) shadowing the DWARF-homed
    `CgsSound::Utils::DataPoint` (CgsSoundUtils.h:31-62: mCurrentValue/mPreviousValue,
    Update/Flush/HasChanged). Leaks into Cgs3dEffectControl.h:101-106.
11. **[BUILD] NFSMix/Nicotine in-build nits**: NFSMixMapState.cpp:62.. 5× dead `!st` guard after
    `&arr[i]` (real guard belongs on `m_pFirstInstance`); IDynamicMixer.cpp:112-114 placement-new on
    possibly-null (asm has the ternary; sibling keeps it); SnapshotMixer.cpp:320 `int laActive[66]`
    unanchored magic + no bound check (stack smash if blob >66 snapshots); NFSMixMapState.cpp:188
    calls the empty AssignMixCtlDataPtrs link-stub then derefs its output (needs guard/FLAG).
12. **[BUILD] AptRenderLinkStubs.cpp:757-1069** — REAL reconstructed Nicotine/NFSMix bodies
    (SnapshotMixer::InitSnapshots @0x82B47350, DestroySnapshots @0x82B46D20, SnapshotVolumeCurve
    @0x82B453C0 + 512-entry LUT, NFSMixMaster::DestroyMainMainMap/AssignSFXCallbacks/InitMixMap,
    NFSMixMap::CreateMainMapState @0x82B49680) homed in an Apt stub file → future dup-def hazard.
    Relocate to Nicotine/SnapshotMixer.cpp + NFSMix/NFSMixMaster.cpp/NFSMixMap.cpp.
13. **[BUILD] stale docs**: all 8 state-manager TUs + BrnSoundLogicModule.cpp still say "OUT of the
    build"; they are mounted — the degenerate-behaviour reasoning is invalid. CMpegBase.cpp:13 stale
    BLOCKED claim (PolySynth IS reconstructed in MpegBase_wM_01.cpp:338).
14. **[BUILD] BrnRootSoundModule.cpp:204** — `StreamHeadersPC::Preload()` PC injection inside a
    decompiled body (well-justified; needs the standard PC-leaf FLAG at site). Low.
15. **[BUILD-adjacent] SharedClasses/Sound/World/BrnStaticSoundMap.cpp:92-94** — hardcoded 16-byte
    entity stride (StaticSoundEntity fwd-declared); consumed via mounted resource-type TU.

## TIER 2 — off-build fabrications / offset hacks (correctness debt)

- **Logic/CgsSoundLogicModule.cpp/.h** (GameShared version) — empty class (sizeof 1) written at
  +0x10..+0x2C74 (≈11KB OOB); fabricated rival `Playback::Module::Module`; 3 invented undefined
  vtable globals. Worst non-mounted TU.
- **Logic/CgsVoicePool.cpp/.h** — ~40 raw offsets across 7 bodies; `reinterpret_cast<Voice*>(p+0x34)`
  at 261/293; PooledVoice modeled as `u8[0x50]` though CgsVoiceWrapper.h + Logic::Voice exist.
- **Logic/CgsEffectBase.h:194-211** — fabricated rival `Logic::State` + wholly INVENTED nested
  `Owner` type (real chain: State::mpStateManager @+0x24 → StateManager::mpLogicModule @+0x2C).
- **ClassTypeInfo<T> tripled** (CgsEffectBase.h:56 w/ extra CreateObject; CgsState.h:57;
  CgsStateManager.h:103) — non-identical defs, genuine ODR. **StateManager tripled**
  (CgsStateManager.h:123 real; CgsStateManagerDtor.cpp:66 rival in .cpp; 
  CgsStateManagerRegisteredContent.h:63 baseless host) + dup ~StateManager in two TUs.
- **Utils/CgsSoundAverage.h:40-85** — rival `CgsSound::Utils::Average<N,T>` vs CgsSoundUtils.h:69.
- **Playback root**: CgsFactory.cpp DoDispose @0x826AD450 (+12/+48/raw vtable slots) +
  CreateContent @0x826AD4D0 (slot 3) + CgsFactory.h:62 fake `Environment`; CgsCommandQueue.cpp:137
  fabricated 2-member AemsPlayerVoice w/ invented mpCommandQueue (ODR vs CgsAemsPlayerVoice.h:114);
  CgsEnvironment.cpp GetV/GetR/new (raw offsets, positional spec reads);
  CgsRegistryResourceType.cpp — NO @0x82 anchor (`@ 0x...` literally) + fabricated positional
  Registry (5 words) contradicting CgsRegistry.h:89 (STALE "faithful" verdict from 2026-06).
- **RWAC**: CgsGenericRwacWaveContent.cpp — fabricated GenericRwacWaveContent+BinaryFileResource_,
  +32/+12 offsets, ODR vs CgsGenericRwacContent.h:149 AND own sibling dtor TU (faithful body is
  `return mLoader.Unload(*this, mContentSpec)`); CgsContentLoader.cpp — 2× +0x0C/+0x30 allocator
  walks + 3 raw vtable casts (by-name route proven in sibling CgsGenericRwacVoiceConfig.cpp:28-34).
- **Plugins/GainArray/CgsGainArrayPlugin.cpp** — UNFLAGGED invented `KF_GAIN_RAMP_STEP = 0.0f`
  (rodata flt_820ADC00 unrecovered) neuters every gain ramp; 2 fabricated types; +0x3000C/+0x30010.
- **GameSource effects**: BrnPresentationEffect.cpp FindFreeVoice @0x82687D68 /
  FindOrStealAVoice @0x826D2AD8 (densest raw-offset walk: this+0x8C stride 0x80, state±0x48);
  BrnInAirEffect.cpp:44-75 pure transliteration; BrnPhysicsControl.cpp:97-107 offset hack + vacuous
  assert (`ptr+0x510 != nullptr`); BrnHybridEngineControl.cpp:37; BrnSubmixesEffect.cpp:62;
  BrnSoundLogicSentinelAccessors.cpp:66 (+ undeclared single-letter externs);
  Passby/BaseEventQueue_PassbyPropByEvent_GetEvent.cpp:24 fabricated PassbyPropByEvent.
- **Logic/Cgs3dEffectControl.cpp** — +0x29A0/+0x40/+0x180 reaches + s32* pun over complete
  MicrophoneSystem (by-name mic API exists).
- **rw/audio Iir2 family + plugins (8 sites)** — `mpInput + 0x28` latency-word hack
  (BandPass/HighShelf/LowPass/LowShelf/PeakingIir2, Delay:310, HighPassButterworth:137,
  ReverbModel1:754) CONTRADICTS the PDB reconcile sitting next to it (`+0x08` is `Voice* mpVoice`,
  Voice +0x28 = `mfFadeStart`); host layout makes the offset land wrong. Root enabler =
  `PlugInBaseView` shadow (Iir2Filters.h:66-81, pre-reconcile names, embedded by 19 plugins).
- **BrnReplaySoundSerialiser.cpp** — "grounded as copy" bodies with NO FLAG (ReadAsQuatPos
  @0x826534E0 / WriteAsQuatPos @0x82658EC8 drop the quat↔matrix math; ReadUpdateFrame @0x826536C0
  drops vmaddfp extrapolation) + ~90 raw offsets in Read @0x826590E0 / Write @0x8265BDD0 that
  duplicate the named StaticLayout header with zero static_asserts (offsets currently agree on x64
  — VEQ<512,16> = 0x210 — but unpinned).
- **Csis/CsisSystem.cpp:116-117** — Subscribe @0x82B0F2A8 truncates host pointer through s32
  (console reloc transcribed literally), unflagged.
- **rw/audio Route/Send vtable sentinels** — one console object off_820AA810 has TWO distinct
  in-tree sentinel addresses (Route.cpp:27 vs Send.cpp:67); vptr compares across TUs diverge.
- **Unresolved externals (link-blockers if mounted)**: Pan2D.cpp KF_PAN2D_* (rodata-recoverable),
  System.cpp CsisMutexLock/Unlock, 4× namespace-scope `GetCpuCycle()`.
- **Playback header "DEFER slice" pattern** (honest but latent): minimal rival re-decls of
  Environment/Voice/Factory/Registry across 10+ headers, some with wrong base class. Single
  consolidation task, not per-TU defects.
- Placeholder data (flagged, ledger): BrnEngines gakPhysicSampleTable all-zero (Physic returns 0s);
  BrnStaticPassbyControl thresholds 0.0f; BrnHUDEffect gfSlipstreamScale; CMpegBase GetBits
  `32-0` shift UB on x64 (logged D1 — real MP3 scalefactor corruption on host); 7 _embed_check TUs
  with `int main()`.

## Recovery levers

- rodata from decrypted XEX (file_off = 0x3000 + vaddr-0x82000000): SelectBin hash table
  dword_83005F24, GainArray flt_820ADC00, Pan2D flt_82F8EEC8/CC, NFSMixShape tables.
- PS3 DecFIGS for declaration shapes; ProStreet PDB for rw::audio names (verify vs ARTIST).
- Playback real headers NOW EXIST for most of the fabricated-type sites — many fixes are
  mechanical include-the-real-header swaps.

---

## WAVE 4 STATUS (2026-08-25, dev f8b46440/3ee53be5/4b9cc6ae/a3d1d7a5/57b09357) — TIER 2 LARGELY RETIRED

DONE: StateManager triple (Dtor TU + RegisteredContent host DELETED, one canonical);
EffectBase invented Owner -> real mpStateManager->GetLogicModule chain; VoiceWrapper real 0x50
layout by name (the 0x5C claim was the POOL stride) + VoicePool ~40 sites + PresentationEffect
Find walk + AgingVoice completed; Iir2 +0x28 family = OWNING Voice's mfFadeStart decay
accumulator (PlugInBaseView renamed to PDB truth, 8 sites by name, GetSize pre-init park
contract; "returns 1" audit claim = false alarm); replay sound serialiser quat<->matrix
reconstructed on the committed UnitQuaternionToMatrix / QuaternionFromMatrix33 idioms +
vmaddfp extrapolation + NEW mfAccumulatedFrameTime@+0x5C (was misbound to mfTime@+0x54) +
StaticLayout static_assert pins; Pan2D 45/135 recovered; Csis truncation FLAG'd (side-table
fix deferred w/ dispatch consumer); RegistryResourceType @0x82667DE8 anchored + de-fabricated;
GenericRwacVoiceConfig/SubmixVoice rival folds.

REMAINING (wave 5): CgsCommandQueue fake AemsPlayerVoice (blocked on GenericRwacVoice chain
merge of 3 partial views); GameShared Logic CgsSoundLogicModule empty-class OOB writes +
invented vtable globals; CgsEffectBase.h ClassTypeInfo leftovers? (folded); the big
minimal-slice headers (Module/CgsSoundPlaybackModule.h 6 rivals, AEMS/CgsAemsPlayerVoice.h 8
rivals incl. Registry/Name -> real CgsRegistry.h, Splicer content/player-voice slices);
BrnInAirEffect/BrnPhysicsControl/BrnHybridEngineControl/BrnSubmixesEffect/sentinel-accessor
offset hacks; PassbyPropByEvent fab; Route/Send dual vtable sentinels; CsisMutex*/GetCpuCycle
externs; BrnStateManager.h's own State rival vs CgsState.h (the reverted re-home — needs the
CgsState.h embedded-Brn-family reconciliation slice).

NOTE: boot smoke blocked late in the wave by an ENVIRONMENT VRAM condition
(D3DERR_OUTOFVIDEOMEMORY with 3.3 GB dedicated VRAM used while the game is closed; identical
stall with changes stashed). Re-run the -skipvideos boot after a reboot.

---

## WAVE 5 STATUS (2026-08-25, dev 620f3c17/bbda1c61/99d65a33) — TIER-2 DEBT RETIRED

Batch A (620f3c17): BrnInAirEffect fake SoundSerialiser-namespace shim retired (real
BrnReplays::SoundSerialiser via the FLAG'd module-tail seam + GetMode/EMode by name);
PhysicsControl vacuous +0x510 assert -> real key guard; SubmixesEffect event-id 15 +
Message<bool> payload by name (CgsMessage.h GetEventId added); ALL 11 sound-logic
&unk_82F2xxxx sentinels identified as ClassTypeInfo records via XEX dump (ObjectIDs
recovered: HUD=0x10/Music=0x20/Fx=0x30, Global=0, StreamingMgr=6, Traffic*=0x30000,
StreamingState=0x60000, Emitter=0x70000, Explosion=0x80000) with G()/S() forwarding to
committed GetStaticTypeInfo; PassbyPropByEvent 64B placeholder homed to its own header.

Batches B–D (bbda1c61): AemsPlayerVoice single merged header (CommandQueue rival deleted;
Stop + mhRequestHandle + FLAG'd CsisCommandQueue route stand-in; 8 collaborator DEFER
slices stay = the AEMS keystone, ledgered); CgsSoundLogicModule footprint reserved
(0x2C78 image, GuiModule maHead precedent — the sizeof==1 OOB-write hazard closed) + real
Playback::Module include + the 3 invented extern vtable globals -> null stores w/ guest
addresses in comments; rw/audio PlugIn.h/Route/Send share ONE gpBasePlugInVTableSentinel;
System.cpp CsisMutexLock/Unlock bodied to Csis::System::Lock/Unlock + GetCpuCycle
__rdtsc FLAG'd PC leaf.

Batch E (the CgsState.h embedded-Brn-family reconciliation — the reverted re-home DONE):
DWARF proves all three leaves `: public BrnSound::Logic::BrnState`; the numeric tails
decode EXACTLY onto DWARF members:
- VehicleState (BrnVehicleState.h now the DWARF struct): nested AttachInfo/EEngineComponent-
  Type, mVehiclePhysicsData@+96 (VehicleData; modelled as leading RaceCarState, FLAG),
  AttachInfo@1216, BoostInfo 36B span@1232, mcaEngineComponentName[2][13]@1268,
  mEngineComponentKey[2]@1296 (8B elements: leading u32 Attribute::Key + un-attested pad,
  the (type+0xA2)*8 walk), mfMaxRpm@1312, mbCollisionOccuredFlag + DataPoint<bool>
  bIsRaceCarActive; ctor @0x826C9E70 moved in; GetEngineComponentKey bodied by name w/ the
  non-zero guard; PhysicsControl mpVehicleState typed + both lookups forward by name.
- StreamingState: `{StreamRequest mRequest; f32 mfFadeOut}` — the manager-ring StreamRequest
  and the state's embedded request are the SAME DWARF type (ctor zeroes it field-for-field:
  5 words + mbDirty byte@104); mbAttached@0x48 == State::mbIsAttached (base); rival
  StreamingRequest deleted; GetRequest now const per DWARF h:166; ctor @0x826B0CB0 +
  ~ @0x826C9B28 moved in.
- PassbyState: `{PassbyStateManager::Passby mPassbyData; f32 mfTimeOutTimer}` — the ctor
  tail = Passby field-for-field (mStaticPos stvx128 zero, mp3dControl, velocity,
  meType=TrafficSmall(3), volume=1.0, suppress); ctor @0x826BF5E0 moved in.
- CgsState.h stripped to the pure State base (+ GetTypeInfo/GetTypeName decl-only virtuals,
  Attach decl-only, IsAttached() bodied on mbIsAttached); BrnState.h rival State DELETED,
  now includes the canonical CgsState.h. 34 TUs gate-clean, lint PASS, full rebuild links
exit 0, -skipvideos boot GREEN (5537 lines, frame 9001 -- the VRAM stall cleared).

---

## WAVE 6 STATUS (2026-08-25, dev a35c4cc7/8ca530cd/72fe8514/eb3a4f3d) — THE RIVAL-FOLD REMAINDER CLOSED

A) Splicer folds: CgsSplicerPlayerVoice.h 3 rivals -> real homes + the REAL PlayerVoice
primary base (dwarfdump prints ONE base only -- zero MI renderings corpus-wide; the dtor
asm destroys Voice@+0 AND GenericRwacVoice@+0x2C -> MI proven; the GenericRwacVoice base
stays a FLAG'd 0x58 span). CgsVoice.h ISlotImplementation reconciled to DWARF (const
Slot&, slot 2 = DoUpdatePlaying not a hole; DoPostAttach/DoPreDetach non-pure -- no
implementer overrides them except the splicer's DoPreDetach). CgsSplicerContent.h's rival
Slot/Voice/PlayerVoice/ISlotImplementation/Splice block DELETED (killed a live compile
tripwire vs CgsContent.h fwd-decls); DoStop/DoPreDetach reach the splice via
static_cast<SplicerPlayerVoice&> (the console's direct voice+0x88 read).

B) HybridEngineControl +0x8C DECODED: the +0x130 slot is the LEAF's own member (base
sizeof == 0x130 per Create @0x826B34E0), DWARF-named mpHybridExhaustControl (h:208) --
the paired exhaust control, whose +0x8C is its own mGinsuRpm.mCurrentValue (ctor
@0x826AF938 DataPoint block +0x7C/+0x84/+0x8C) == GetGinsuRPM() (h:177, added). The old
model byte-viewed the BASE's mpEngineControl -- wrong member. All sibling void* controller
back-pointers typed per DWARF (HybridExhaustControl 4, ShiftControl 3). EngineControl's
full X360 ctor layout decoded as side evidence (@0x826B2A98: ptrs 0x3C-0x4C, DataPoints
0x50/0x58/0x60, mAudioPitch@0x68, mAudioDistortion@0x84).

C) CgsSoundPlaybackModule.h 4 rivals folded onto real homes: Ge==Environment::GetFactory
(DWARF h:280, added), GetV==GetVoice (renamed); rival Voice::SetSlotName == the real
Voice::SetIdent (the +0xC word is mIdent -- "slot name" was invented vocabulary);
AttachVoice's content param is Handle<Content> (DWARF h:345); ConnectVoice uses the real
Voice::Connect(Name, Handle<SubmixVoice>&) @0x826ACC90; Module::C renamed CreateVoice
(DWARF h:318, sret-lowered); Factory::CreateVoice<T> (DWARF CgsFactory.h:91) + Voice::
Attach (DWARF :501) + Registry::Dump (h:137) added to the real homes; member renames
mhReserved->mhSplicerFactory / mCommandLock->mStreamMutex / mpStringTableHead->
mpStringTable; the +0x22C tertiary vptr identified as the DWARF `: protected
IContentLoadService` base -- the exact sub-object pointer CreateContent wires into each
new content's mpLoadService; ImportStringTable documented as the DWARF (const Registry&)
serialized view.

D) CgsAemsPlayerVoice.h 7 collaborator rivals folded (asm calls the real symbols); the
VoiceSpec mu8TailUnitCount rival member was an INVENTION (operator new reads the real
+0xC mu8SendCount byte); the CSIS lhz +0xC is mu16UserParameterStart NOT a parameter
count; AemsVoiceCsisClass grown to the DWARF data entity (the committed home had
conflated it with its EntityFixer); VoiceSchema::GetFeatureSchema + VoiceSpec::
GetVoiceSchema added decl-only.

REMAINING LEDGERED (all keystone-gated): the AemsFactory base chain + typed registry +
CsisCommandQueue route (retires GetRwacFactoryRegistry/GetAemsFactoryRegistry/
GenericRwacFactorySkName/HACK_SetSnd9InitSubmix + the AemsPlayerVoice/SplicerPlayerVoice
GenericRwacVoice base spans); the Playback engine grow-in (Module Prepare/Update + the
tri-vtable MI shape); Environment::GetR vs DWARF GetRwacVoiceByPlugin return-type
reconcile; the Csis dispatch side-table.

---

## FAITHFUL-AUDIO-ENGINE CAMPAIGN, PHASE A (2026-08-25, b5-decomp dev 3d1e8b5c) — THE MIX ENGINE IS LIVE

Plan: C:\Users\JeBobs\.claude\plans\sparkling-petting-hopper.md (items 1/2/4/5/6 of the
engine-sounds track; item 3 = content loading stays out of scope). Phase A = mount + RWAC
init: the vendored rw::audio::core subset + CsisSystem are in the exe, the XDK XMA HAL has
a software decoder over the FFmpeg xmaframes path (CgsXmaHardwarePC.cpp), the four
DecoderDesc records are XEX-recovered, System::Alloc XEX-decoded (@0x82B6BE18, the "flags"
param is an allocator override), Xas1Dec::DecodeChannel RECONSTRUCTED (de-BLOCKED), the
rw/audio TUs moved to the vendor EA::Thread::Mutex, and RootSoundModule's RWAC + LOGIC
carve stages are REAL. Boot: `[rwac] System::CreateInstance -> LIVE (bank 0x18 carve
present)` -- the allocator gate was already open. Boot green 5624 lines.

LEDGERED from phase A: the REAL TestBed::Allocator::DoAllocate (@0x826AE420, 441-line
tracked carve -- an honest pass-through stands in); the no-coalesce heap tuning + the
CsisPrivateHeap sub-allocator (GeneralResourceAllocator factory surfaces); the
DecoderDesc callback-slot typing (needs the descriptor-dispatch consumer); the
RwacCoreAllocatorBridge host seam. NEXT: phase B (the Logic/Playback Module keystone;
re-export dossiers 0x826E9700 + 0x826EBA08 first).

---

## PHASE B1-B4 (2026-08-25, b5-decomp dev 0742d704 / 534ce67e / 0648a7e6) — THE MODULE KEYSTONE + THE PER-FRAME PUMP

B1+B2 (0742d704): both Module keystones got their REAL shapes. Playback Module derives
the real ModuleSingleBuffered with the full DWARF member list (zero-gap closure vs the
Construct/Prepare/Update/Release asm; the +0x2298 "reserved" was the
VariableEventQueue<1024,16> deferred queue); Construct(int)/Release/Destruct bodied.
Logic Module ditto (Environment @+0x2950 with the real ModuleParams copy in
PrepareProxies; the [type+1]-slot mistake fixed to direct [type] indexing);
Construct/Prepare/Update/ProcessMessageQueue/Attach/DetachBuffers + both operator++
bodied; the Logic Environment Construct/Update + LogicMixerAllocatorBridge landed.

B3 (534ce67e): the Playback Environment allocation chain — private ctor @0x826BFBC8
(handle tables placement-new'd at the co-located carve tail + the in-place Registry),
Create @0x826C7948, GetAllocatedSize, the operator-new registry stride fix — and
Module::Prepare @0x826E90C0 (the 5-rung machine, the DWARF-constant spec
{4/192/128/2048/388608}, the stream-buffer carve, the 8 environment CPU monitors).
FLAG (the AEMS keystone): stage 3's three factory creates (RWAC @0x826C7AD0 / AEMS
@0x826DAC28 / Splicer @0x826DB130 -- specs + carve formulas documented at the site)
stay DEFERRED with the factory-ctor cascade; the handles are null until that slice.

B4 (0648a7e6): the interface bases are REAL — IStreamProvider (+0x228, NEW
CgsStreamingPlugin.h home; DoOpenStream @0x826FA020 / DoCloseStream @0x826FA2B8) and
IContentLoadService (+0x22C, CgsContent.h:70; DoServiceContentLoadRequest @0x826F9F88)
— the tri-vtable MI shape is CLOSED. The Io::InputBuffer/OutputBuffer pair (NEW
CgsSoundPlaybackModuleIO.*), Module::Update @0x826E9700 (the per-frame pump),
ProcessResourceReceiverQueue @0x826A25C0 (events 4/16/18), UpdateStreamBuffers
@0x826A28E8 (the 0xF0 stomp scan + grace release), FindStreamBuffer @0x82689CE0; the
StreamBuffer status machine got its real DWARF names (the "4" seed == E_FREE_BUFFER).
Environment::Update @0x826D7500 + UpdateContent/UpdateVoices/UpdateFactories (the
Voice vtable labels were one slot off — Object's [dtor, DoDispose] head precedes
GetCpuTicks/GetProfileVoiceType); Content::Update @0x82680BA8 bodied in the mounted
CgsObject.cpp. ECOSYSTEM: CgsResourceIOEvents' miField0/miField4 identified as
FileEvent::{mpUser,miEventId} + the full OpenStreamRequest tail + the
ReadStreamEvent/CloseReadStreamRequest family; CgsContentLoader's raw-vtable dispatch
became the real DoServiceContentLoadRequest virtual; Playback::System typedef'd to the
real rw::audio::core::System; Factory::Update(f32) public front made inline.

Boot green after each commit. NEXT: B5 (un-gate RootSoundModule: Construct steps
[3]/[5]/[6], the real PLAYBACK_MODULE Prepare with the bank-9 carve + the scratch IO
pair) then phase C (the pump seam at BrnGameMainFlowStates).

---

## PHASE B5 (2026-08-25, b5-decomp dev 176e2656 + parent bat 21ec92ab) — THE ENGINE IS MOUNTED END-TO-END

RootSoundModule un-gated onto the real Module keystones: SoundLogicModule derives the
REAL CgsSound::Logic::Module engine base (primary, console +0 — its mPlaybackModule
@+0x238 IS root+0x4B8) + IResourceRequester (+0x4C90, closing the wave-1 GetResource-
Registrar +0x588 riddle: 0x4C90+0x588 == +0x5218); the by-value lEnvironment retired
for the engine base's own; Prepare @0x82703C18 is the real console stage machine
(engine-base Prepare with the XEX-recovered ModuleParams::DEFAULT {16,16,16}
@0x820AA480 — now DEFINED; bridge/boot-prepare under write locks; one-chunk-per-call).
Root Construct [3] real (playback Construct(6)); the PLAYBACK Prepare stage real
(bank-9 carve -> gPlaybackTestBedAlloc + System::Lock + playback Prepare; the
audio-stream LinearMalloc handed as null while un-Created — FLAG'd — so Prepare takes
its real 0x20000x4 main-allocator stream path); all 3 scratch IO buffers real.

MOUNT CLOSURE (the predicted cascade, all resolved): 8 TUs mounted (engine
CgsSoundLogicModule / Playback CgsEnvironment+Module+IO / CgsGenericRwacFactory /
CgsFactory / CgsVoice / CgsContentSpec) with the device.cpp-pattern OBJ-NAME COLLISION
fix for the two Logic-vs-Playback basename pairs (⚠️ same-basename TUs in the flat obj
dir silently drop one TU — the LNK4042 trap); reals: RwacSystemLock/Unlock +
GetDefaultRwacSystem bodies, Factory::GetName inline + CreateVoice<T> (CreateContent's
attested mirror) + the base Do* defaults, Voice::GetSlot/GetSend/GetInputParameter
(the GetOffsetObject pattern), Handle<T> adopt/assign/validity/arrow inlines (interim
plain-store ref-model), engine GetUniqueId; ⚠️ MessageHeader fwd decls were
struct-keyed vs the class definition — MSVC mangles the KEY, so Notify resolved to a
DIFFERENT symbol (unified to class; same fix for SubmixVoice/VoiceSpec fwds).
Deferral stubs (BrnBaselineLinkStubs, inert until item-3/phase-D): Voice::Attach +
base virtuals, Slot::Update/Detach, Content::OnAttach, Environment::GetFactory/
Allocate/op-delete, Registry::Dump, the 4 factory shims, gu32VoiceTypeTag/
gu32NamedSlotSentinel (un-decoded interns, zeroed), ContentSpec/VoiceSpec SK_TYPE_NAME,
Logic Environment::Notify, RwacPlugInEvent; ContentType::GetContentClass carried as an
exact copy (CgsDataStructures.cpp stays unmounted — its EntityFixer hooks need the
declared-only Entity member-pointer fixup templates).

BOOT: green 6045 lines — the console's own "Sound Playback - Environment (+ Registry)
is 407792 bytes in size." print is LIVE at loading stage 4; GuiSound/ScriptedLoad/world
all advance; zero new asserts. (Two interim runs parked at title-attract ~444 lines —
REPRODUCED with the changes stashed on the B4 baseline == the known wave-4 environment
flake, cleared on its own.)

NEXT: phase C (the per-frame pump: PreUpdate chain + RootSoundModule::Update + the
world bridges + the BrnGameMainFlowStates seam), then the factory-ctor cascade (the
AEMS keystone) to light stage-3's three factory creates, then phase D (Dac ->
AudioOutputPC).

---

## PHASE C1+C2 (2026-08-25, b5-decomp dev 8eddd498) — THE PER-FRAME PUMP BODIES

C1: the +0x1824 "trailing state" on BOTH root/logic output buffers IDENTIFIED as the
11-slot BrnReplays::ReplayIO::RequestInterface (44 == sizeof; 6224 == 0x1824+0x2C total
closure) — BridgeLogicToRoot @0x826EBF18 merges it via RequestInterface::Append
@0x823A6868; both provisional RequestInterface<4096> typedefs retired. The PreUpdate
chain: RootPreUpdateOutputBuffer::Construct bodied (the 824-byte carve attests the
GuiOut storage as VariableEventQueue<256,16>); NEW Io::LogicPreUpdateOutputBuffer
(DWARF: empty-derived); SoundLogicModule mPreUpdateOutput block (+0x4EB0) +
GetPreUpdateOutput + PreUpdate @0x826E1F10; RootSoundModule::PreUpdate @0x826EB928 +
BridgeLogicToRootPreUpdate + BridgeLogicToRoot; the Logic output buffer's
resource/replay accessor pairs bodied.

C2: RootSoundModule::Update @0x826FB238 bodied (DWARF 7-arg signature w/ trailing
BrnUpdateSet): scratch trio, the miLogicUpdate-bracketed engine logic Update,
BridgeLogicToRoot under the console lock set, Playback::Module::AdvanceTime (the
+0x2700/+0x2704 time pair the Environment consumes), the playback Update virtual, the
playback-output appends (root 4096 interface + the NEW SoundLogicModule
mFreedStreamBufferIds Array<u32,3> == X360 root+0x5460), the "Possibly SPU crashed."
high-water assert. Debug tail (Statistics/SanityChecks/dump one-shot) gated on its own
slices. Runtime-inert until C4 wires the callers. Boot green 4749 lines.

DECODED FOR C3/C4 (next batch):
* The console spine LoadingScriptedState::Update @0x823F22D8 (631 instrs) creates
  RootIn/RootOut ("Sound") + RootPreUpdateOutputBuffer ("SoundRootPreUpdateOutput") and
  orders: DoPreUpdate_Sound (wraps RootSoundModule::PreUpdate) ... BridgeSoundToWorld
  (pre-world) ... world update ... BridgeGuiToSound + BridgeWorldToSound ...
  RootSoundModule::Update ... then the sound->resource forward off RootOut.
* BridgeSoundToWorld @0x823CDC98 = ONE append: worldInput audio-car queue <-
  rootPreUpdate.GetPreUpdateOutput().mAudioCarDataLoadedQueue (+0x110).
* BridgeWorldToSound @0x823CD580 = ~8 RootInputBuffer Set* pointer installs from the
  world UpdateOutputBuffer accessors (vehicle data picks source off updateSet bit
  0x100; contact spy / traffic / physical traffic +9760 / deformation / world-load /
  AI car + more) — needs the RootInputBuffer Set* surface.
* BridgeGameStateToSound @0x823CDE50 = the 13312 game-event queue append + 3 interface
  installs (gamestate out +176344/+173240/+175976) + SetUpdateInfo(bool from game
  module +10094119/20).

---

## PHASE C3a+C3b (2026-08-25, b5-decomp dev 50d101c0 + 3a894c84) — ALL THREE WORLD BRIDGES REAL

C3a: BridgeSoundToWorld @0x823CDC98 (one append; the world UpdateInputBuffer write
accessor added). C3b: BridgeWorldToSound @0x823CD580 (the vehicle-data copy w/ the
bit-0x100 replay-source select, six interface installs incl. the FLAG'd vehicle-output
+9760 physical-traffic reach, four queue appends) + BridgeGameStateToSound @0x823CDE50
(the 13312 game-action append, three interface installs, the UpdateInfo byte off the two
un-decoded +1009411x game-module flags — named by offset, false at boot).

STRUCTURAL WINS: the DWARF `typedef RootInputBuffer LogicInputBuffer`
(BrnCollisionStateManager.h:402) retired the separate LogicInputBuffer struct AND its
under-sized 0x40 vehicle-interface reinterpret (a latent overread); RootInputBuffer got
the REAL by-value RCEntityActiveRaceCarOutputInterface mVehicleData @+0x620 (the 10480
XMemCpy width abuts +0x2F10 exactly; members after it name+sequence per the
PreUpdateOutput precedent) + SetVehicleData @0x823B87C0 (copy + h:967/:980 tripwires +
the guarded player-index republish) + its own audio-car queue @+0xED50 (abuts +0xEEE0
exactly) + the prop-update queue storage WIDENED to the attested 0x3210 span (the old
nominal 0x40 under-sized the append target); the world UpdateOutputBuffer's two
remaining read accessors (@0x823B67E8/@0x823B69E0); the gamestate GameMode interface
carved @+176344 + accessor. ⚠️ LESSON: "(own TU)" deferral notes go stale —
IsPlayerCarActive/GetPlayerActiveRaceCarIndex were ALREADY bodied (rival inlines
collided; reverted to decls).

Runtime-inert until C4. Boot: loading DONE + attract healthy, zero new asserts (the
title-attract park recurred — the session's stash-proven environment flake).

NEXT: C4 — wire the spine (LoadingScriptedState::Update @0x823F22D8 decode in the C1/C2
notes above): RootIn/RootOut ("Sound") + RootPreUpdateOutputBuffer
("SoundRootPreUpdateOutput") buffers, DoPreUpdate_Sound -> BridgeSoundToWorld (pre-world)
-> BridgeGuiToSound + BridgeWorldToSound (post-world) -> RootSoundModule::Update -> the
sound->resource forward, with the miUT_Sound/miUT_SoundUpdate perfmon brackets. Then the
AEMS factory-ctor cascade, then phase D.

--------------------------------------------------------------------------------
2026-08-28 — PHASE C4a DONE (dev 15f4acf7 + parent bat): the LOADING-SPINE pump is LIVE
--------------------------------------------------------------------------------

The engine is RUNTIME-ACTIVE: LoadingScriptedState::Update drives the console's own
sound legs every frame (all pre-game flow states). Full implementation-grade decode of
0x823F22D8's sound legs: progress/scratch_dossiers/spine_sound_leg_0x823F22D8.md
(agent-decoded, register-level); the in-game leg DoUpdate_Sound @0x823DCEC0 decode:
progress/scratch_dossiers/doupdate_sound_0x823DCEC0.md (ready for C4b).

LANDED: the frame trio (RootIn "Sound" INPUT stack / RootOut "Sound" +
RootPreUpdateOutputBuffer "SoundRootPreUpdateOutput" OUTPUT stack, teardown in exact
reverse order); the stage-6 cue posts into the frame's OWN RootIn (per-cue carve
retired); DoPreUpdate_Sound @0x823EE4D8 bodied whole (perfmon pair miUT_Sound/
miUT_SoundUpdate = first-ever users, PreUpdate, GuiOut Append<256,16> into the GUI
input's 32768 queue, BridgeSoundToTraining, the console W(guiIn)+R(preUpdateOut)
bracket); BridgeSoundToWorld staged inside DriveWorldUpdateFrame (threaded through
UpdateWorldModule — the shared site both spines use); BridgeGuiToSound (unconditional)
+ BridgeWorldToSound (stage>5, update set feeds the 0x100 replay select);
RootSoundModule::Update with the console's exact f32 products (gameTimer/simTimer
[+0x10]*[+0xC] == mfScaleCurrent*mfRate); the TWO sound->resource forwards (AttribSys
2048 + request 4096). Mounts: GameBridgeSoundToX.cpp whole; BridgeGuiToSound MOVED to
new sibling GameBridgeGUIToX_Sound.cpp (the parent's six un-homed symbols stand).

THE CONSTRUCT FIND: the first live BridgeWorldToSound append fired "Not Constructed"
on the 1536 game-event queue -> the REAL RootInputBuffer::Construct @0x826C81D8
decoded whole (ten sub-constructs + inline default stamps). mGameEventQueue TYPED
(opaque u8[4] -> real VEQ<1536,16>, Construct @0x822C6F78 + the bridge twin both
attest); the two prop queues Construct'd via EventQueueHeaderView (the attested
three-store queue Construct on the host header shape the appender casts already rely
on); mVehicleData cleared by its OWN Clear @0x8227D550; ⚠️ CORRECTED: the console
seeds mePlayerActiveRaceCarIndex = literal 0 (E_ACTIVE_RACE_CAR_INDEX_0), NOT the -1
INVALID that C3b used. Still-opaque ctor calls FLAG-deferred with addresses (camera
@0x82255E68, contact-spy @0x82A61A18, deformation @0x8228F1B0, world-load @0x822E50D0,
physical-traffic @0x8228DB90, the maPad1 string-head clears + {-1,-1,flt} @+0x610, the
35-slot AI-car defaults loop — no PC reader yet / overwritten per bridged frame).

BOOT: GREEN — 9855+ lines, ZERO asserts, full stage ladder 1->6->8 with the pump
consuming its own cue buffer, flow reaches in-game 0x88, world at 170 fps. (One
title-attract park run in between = the known environment flake.)

DECODE NOTES BANKED (both scratch dossiers): the loading spine has NO
BridgeGameStateToSound (its ONLY caller is DoUpdate_Sound @0x823DCEC0) and ZERO
spine-level perfmon; DoUpdate_Sound's 11-arg signature + bridge order
(WorldToSound -> SetCameraInput(directorOut gm+0x9A0BDC +0x180 via @0x823B3308) ->
GameStateToSound -> GuiToSound(gm+0x9A0BD8) -> SetReplayStatusInterface(+0x4 via
@0x823BB080)) + its self-carved RootIn + NO resource forward (DoUpdate's tail drains
rootOut ~0x823F1F08-48); BridgeSoundToTraining PC body verified FAITHFUL vs the asm.

NEXT: C4b — DoUpdate_Sound @0x823DCEC0 bodied + the in-game drive
(MainGameFlowStateInGame::Update / DriveInGameWorldUpdate threading: preUpdateOut +
rootOut carves, DoPreUpdate_Sound, BridgeSoundToWorld via the world drive,
DoUpdate_Sound post-world, the caller-side resource drain). Then the AEMS
factory-ctor cascade, then phase D.

--------------------------------------------------------------------------------
2026-08-28 — PHASE C4b DONE (dev 760274ed + parent bat): C4 COMPLETE — both spines pump
--------------------------------------------------------------------------------

DoUpdate_Sound @0x823DCEC0 bodied whole (the real 11-param signature — the IDA 32-arg
prototype was a Xenon stack-slot artifact; decode
progress/scratch_dossiers/doupdate_sound_0x823DCEC0.md): perfmon pair; self-carved
RootInputBuffer "Sound" (INPUT stack, CgsModuleIOHelper h:52/57 asserts); the
six-buffer lock helper + the interleaved LOCK-ONLY effects bracket (kept faithfully);
bridge order WorldToSound -> SetCameraInput (director +0x180 -> rootIn+0x2F20, FLAG'd
4-byte model) -> GameStateToSound (only console caller) -> GuiToSound ->
SetReplayStatusInterface (+0x4); locks released before Update; NO resource forward
(the caller's tail drains rootOut). In-game frame wired in
BrnGameMainFlowInGameState.cpp per the FULL DoUpdate walk decode
(progress/scratch_dossiers/doupdate_spine_codex.md — codex agent: the complete
0x823F0AF8 call stream incl. all 10 buffer creates, the 18-leg scheduler order, the
22 memory-audit blocks, the resource-forward tail, the reverse destroy batch):
rootOut+preUpdateOut carves, DoPreUpdate_Sound pre-world (guiIn == the gm+0x9A0BCC
static), preUpdateOut threaded to the BridgeSoundToWorld staging, DoUpdate_Sound
post-world (replay/effects null-FLAG'd), caller-side forwards, reverse teardown.
Mounts: GameBridgeGameStateToX_Sound.cpp (MOVED split) + BrnReplayModuleIO.cpp
(GetStatusInterface EXISTS — the DoUpdate_World banner note was stale) +
GetDirectorOutputBuffer accessor (gm+0x9A0BDC).

BOOT GREEN with the leg LIVE: 23412 lines, ZERO asserts, in-game 0x88, the eye
MOVING through the streamed world at 165 fps to frame 16441 — the pump + all five
bridges every in-game frame. C4 (items 4+5) COMPLETE.

IN FLIGHT (codex, background): the AEMS factory-ctor cascade decode
(progress/scratch_dossiers/aems_factory_cascade_codex.md when it lands) and the
phase-D Dac plugin decode (progress/scratch_dossiers/dac_plugin_decode_codex.md).
NEXT: the AEMS cascade (lights the stage-3 factory creates), then phase D
(Dac -> CgsDacOutputPC SetEngineFill; SinePlayer audible probe).

--------------------------------------------------------------------------------
2026-08-28 — AEMS-CASCADE SLICE 1 (dev 35befa4b + parent bat): the RWAC factory is REAL
--------------------------------------------------------------------------------

GenericRwacFactory create @0x826C7AD0 + ctor @0x826C17A0 bodied whole (spec ABI,
off_83271928 resolution, the console carve formula at host widths through the
ENVIRONMENT allocator, the in-place +0x4020 Registry, the RwacLock bracket with
its never-before-bodied ~RwacLock unlock tail); stage 3 lights create [1/3] into
mhRwacFactory; the REAL "~GenericRwacFactory::SK_NAME~" intern (writer
sub_82C654A8) retires the default-Name placeholder AND gives gu32VoiceTypeTag its
real value; GetRwacFactoryRegistry real; the DECODER pass live
(GetDecoderRegistry @0x82B6DD78 bodied on the vendor System from a re-exported
dossier; standard set + Pcm16Big — the decoder registry now holds
Xas1/Xas/EaXma/Pcm16Big at boot). 24 vendor plug-in TUs mounted + 4 FLAG'd
Feb-2007-absent closure stubs + SubMix_statics.cpp minimal home.

⚠️ THE PLACEHOLDER-DESCRIPTOR LESSON (measured live, caught, documented): every
vendor GetPlugInDescRunTime returns a single-pointer PLACEHOLDER, and
RegisterPlugInRunTime writes record link fields ~40 bytes in — registering them
SCRIBBLED the loading-stage table + menu-music stream headers ("loading stage
<garbage>" + a StreamHeadersPC crash). The 25-record plug-in pass is deferred at
the site in exact console order; it lands when PlugInDescRunTime's field layout
is decoded + per-plugin HOST records built (codex task in flight →
progress/scratch_dossiers/plugindesc_layout_codex.md). An empty plug-in registry
= GetPlugInHandle null = guarded voice-create failures, the honest degraded
state.

RE-EXPORTED DOSSIERS (the cascade's research blockers, all gone): VoicePool::
Prepare @0x8268AC40 (decoded: 0x40-cap pair-copy + free-stack, count/top at
+0x300/+0x304 — the 0x308 pool stride confirmed), System::GetDecoderRegistry
@0x82B6DD78, GainFader @0x82B97368 + LowPassButterworth @0x82B97BF0 getters.

BOOT GREEN: 15013 lines, zero asserts, zero corrupt-stage lines, in-game 0x88.
(Run ended by external window close — normal shutdown; one FIRST-CHANCE
STATUS_HEAP_CORRUPTION at CRT exit, unobservable under force-killed runs, filed.)

ALSO BANKED: the Effects-leg decode (progress/scratch_dossiers/
effects_leg_decode.md — DoUpdate_Effects @0x823DD0A8 whole, BridgeRendererToEffects
@0x823C1168 four installs from DoDispatch, EffectsModule::Update @0x8229EC28
vtable-resolved, the mount worklist incl. the BrnGameModule.hpp:138 empty-
placeholder ODR fork) and the AEMS rodata recovery (codex →
progress/scratch_dossiers/aems_rodata_recovery_codex.md, in flight).

NEXT: cascade slice 2 = AemsFactory (the AemsRWSampleFactory MI correction +
ctors + the CsisPrint static-callback ABI fix), slice 3 = Splicer (ctor +
SpliceManager + VoicePool::Prepare + the voice helpers + off_82FFBA0C publish);
then the plug-in descriptor records; then phase D (Dac decode in flight).

--------------------------------------------------------------------------------
2026-08-28 — AEMS-CASCADE SLICE 2 (dev 3f67ebf5 + parent bat): the AemsFactory is REAL
--------------------------------------------------------------------------------

AemsRWSampleFactory MI CORRECTED ({IAemsSamplePlayerFactory @+0, Factory @+4} —
the ctor @0x826C26B8 asm proof vs the dwarfdump single-base rendering; the
wave-6 lesson applied again) + its ctor bodied (registry cache, the 'Sen0'/'Sub0'
handle lookups, Xas/Xas1 decoder registrations, the three TYPED PlugInConfig
records with exact console values; the six desc REGISTRATIONS FLAG-deferred on
the placeholder-descriptor hazard). AemsFactory rebuilt on the real base
(console member set name+sequence, in-place Registry @+0x56C) with Create
@0x826DAC28 (ref-spec, 4*(entities+0x162) carve, Factory-subobject refcount) +
ctor @0x826DAAD0 (RWAC retain, CSIS IsInited assert, the @0x82B0F1B8 no-op-leaf
NOT promoted into an invented registry, SetSamplePlayerFactory(this) into
off_82F87DBC); CsisPrint STATIC (ABI corrected); GetAemsFactoryRegistry real;
the "~AemsFactory::SK_NAME~" intern (dword_83008664/sub_82C65788). Prepare
stage 3 create [2/3] REAL into mhAemsFactory. Mounts: the two AEMS interface
TUs + sndaems.cpp + sndaems_linkclosure.cpp (the three content-teardown externs
stubbed); the player control-surface virtuals FLAG-stubbed (no player
constructible yet). ⚠️ `struct System` fwd decl vs the CgsVoice.h:59 typedef =
C2371 — spell the typedef, never fwd-declare Playback::System.

BOOT GREEN: 9107 lines, in-game 0x88, zero corrupt stages; one PRE-EXISTING
gameplay assert (DriveThruManager slowmo tripwire, hit by the roaming demo
drive, run continues). NEXT: slice 3 Splicer (ctor + SpliceManager + the
re-exported VoicePool::Prepare + voice helpers + the off_82FFBA0C publish).

--------------------------------------------------------------------------------
2026-08-28 — THE DESCRIPTOR-RECORD WAVE (dev fcbf3961 + parent bat): 12 plug-ins REGISTER
--------------------------------------------------------------------------------

PlugInDescRunTime FULLY TYPED (the 52-byte record, field-by-field consumer proof
+ all 25 console records XEX-recovered with per-callback PC-body status — codex →
progress/scratch_dossiers/plugindesc_layout_codex.md). The two consumer local
views (Voice.cpp VoiceStageDesc / PlugIn.cpp PlugInFactory) RETIRED — the
console-offset pads would misread host records. TWELVE real host records replace
the placeholder statics (the scribble source): AiffWriter/BandPassIir2/Gain/
HighPassButterworth/HighShelfIir2/LowPassIir2/LowShelfIir2/Pan2D/PeakingIir2/
Rechannel/ReverbModel1/Send — XEX-exact fourccs + tail bytes, host callbacks
(ICF-shared GetSizes carried as on console; 6 member-Process plug-ins dispatch
via static thunks). RWAC ctor registers the 12 in console order (each hole
FLAG'd + reasoned); AemsRW ctor's Gain/Pan2D/Rechannel live + the 'Sen0' handle
lookup resolves REAL. Mounts: Delay/RawPuller2/DelayFilter + 2 unreachable
declared-only stubs. SECOND re-export batch done (7 dossiers: the five missing
plug-in bodies + SubMix::CreateInstance + the SnP1 preprocess) — the decode is
in flight (codex → plugin_callbacks_decode_codex.md), which completes
Limiter1/Pause/Resample/HighPassIir2/Pan2D1 registration next.

BOOT GREEN: 7628 lines, ZERO asserts, zero corruption, in-game 0x88.

NEXT: the five decoded bodies → +5 registrations; cascade slice 3 Splicer
(mono voices need SnP1 — SndPlayer1's own home is the remaining gate for real
splice voices); phase D Dac (codex decode still running).

--------------------------------------------------------------------------------
2026-08-28 — AEMS-CASCADE SLICE 3 (dev 637bd4f6/303666af + parent bat): THE CASCADE IS COMPLETE
--------------------------------------------------------------------------------

All THREE stage-3 factory creates run at boot — the AEMS keystone deferral
(open since B3) is structurally CLOSED. SplicerFactory Create @0x826DB130 +
ctor @0x826DB010 real ("~SplicerFactory::SK_NAME~" intern dword_83008404/
sub_82C65938; in-place Registry @+0x1C; the trailing SpliceManager arena;
SplicerAssertFunc STATIC into manager+0x610). SpliceManager ctor @0x826C30C8
bodied (containers, sink, env save, off_82FFB9F0 publish, the four handle
lookups in the asm's exact order — 'Sen0' REAL); VoicePool::Prepare @0x8268AC40
from the re-exported dossier; the +0x6C4 console-ABI Heap view RETIRED for the
DWARF `const Environment&` + the real GetAllocator()->DoAllocate path (the
flagged divergence fixed). FLAG: voice staging deferred (SnP1/Rsp0/Pn21 null
until their plug-ins register; pools park EMPTY, free stacks -1, guarded null
pops; stage-0 floats 1.0/2.0 XEX-recovered and documented at the site).
Prepare stage 3 [3/3] real into mhSplicerFactory + the off_82FFBA0C publish
(the module's IStreamProvider sub-object; global defined in the publisher TU).

BOOT GREEN: 16019 lines, in-game 0x88, zero corruption; one PRE-EXISTING
DriveThruManager slowmo assert (gameplay-side).

NEXT: the five decoded plug-in bodies (codex plugin_callbacks_decode in flight)
→ +5 registrations → light the splice-voice staging (needs SndPlayer1's home
for 'SnP1'); phase D Dac (codex decode still running).

---

## 2026-08-28 — PHASE D: the Dac output plug-in + the Mixer executive + the PC engine fill (b5-decomp dev 99ba1816)

**The output bridge is LIVE (silence gate PASSED).** The engine now pumps one
256-frame mix through the reconstructed console path per device buffer.

* **rw::audio::core::Dac** (vendor plugins/Dac.{h,cpp} NEW): every body
  store-for-store — CreateInstance @0x82BA24A0, GetSize @0x82B96CB0, desc getter
  @0x82B96DB8 (the real 'Dac0' record off_82F8C7A8), EventEvent @0x82BA27F0
  (vt[1]; mode query + 4 deferred commands at HOST strides), Start/StopImmediate
  @0x82B96DC8*/@0x82B96E38, the four handlers @0x82B9DCF0*/DD48/DB78/DCE8,
  RampOutput @0x82B96CB8, XenonDownMix @0x82B97178, Process @0x82B97250, Mix
  @0x82B96E80*, ReleaseEvent @0x82B9DAE0 (the dtor). (* = recovered from raw XEX
  words — exporter gaps.) ReOrderRwAudioCoreToWave @0x82B6B590 + ClipFloats
  @0x82B64B68 homed beside their one consumer. Full decode:
  scratch_dossiers/dac_plugin_decode_codex.md (session-cross-checked).
* **PC = the console's own EXTERNAL-DAC configuration**: every XAudio/worker
  site guarded by the console dword_8327EE58 flag, defined 1 on PC (FLAG'd);
  XenonThread @0x82B96F40 documented, not ported.
* **rw::audio::core::Mixer** (vendor Mixer.{h,cpp} NEW): ctor @0x82B6D880
  (typed StackAllocator seeded over the 0x5800 arena) + Execute @0x82B6D900
  (SampleBuffer re-seeds + the active-voice stage walk + CPU stats; 0 with zero
  voices → silence). ⭐ AudioProcessContext/AudioChannelBuffer/AudioFormat ==
  Mixer/SampleBuffer/MixerExecuteParams (Iir2Filters.h typedefs onto the one
  host type). ProcessInputPlugIns @0x82B6A048 + HandleBufferStatusUnavailable
  @0x82B69F78 FLAG honest stubs — the register decode LANDED
  (scratch_dossiers/mixer_voicepath_decode_codex.md) and bodies follow next.
  CullVoices @0x82B6E410 FLAG stub (no dossier; unreachable). Voice stat fields
  renamed to their Execute-grounded semantics.
* **PlugIn::Event** widened to the 3-arg console shape; RwacPlugInEvent bodied
  real (stub retired). RWAC registration 3 LIVE (13/25).
* **The PC bridge**: AudioOutputPC::SetEngineFill (4th additive persistent
  slot) + CgsDacOutputPC.{h,cpp} (the engine fill inverting the XenonThread
  packet loop: lock → ExecuteCommands → Mix → 6→2 equal-power fold + f32→s16,
  or silence). The FLAG'd D3 seam in Module::Prepare stage 3: generic
  descriptor-path create → Environment::SetDacPlugin (FLAG'd setter; console
  writer = the unexported data-driven DAC-voice create) → Attach → StartDac.
  Console start trigger (SoundLogicModule::ProcessGuiEvents @0x826ED6C8) later.

**BOOT GREEN** (-skipvideos, 100 s, 18146 lines): stage-4 SoundModule opens the
device at 48 kHz through the seam; the audio callback pumps
ExecuteCommands+Mix every ~5.3 ms sustainably; zero NEW asserts (the
DriveThruManager slowmo tripwire = the known ledgered item); the roaming demo
drives into gameplay.

**NEXT**: ProcessInputPlugIns + HandleBufferStatusUnavailable real bodies (the
landed voicepath report) → the SinePlayer audible probe (gate D's second half)
→ phase E (the five decoded callbacks are also banked in
plugin_callbacks_decode_codex.md).

---

## 2026-08-28 — GATE D CLOSED: the audible proof (b5-decomp dev 322d69cc / d4fd28c0 / 035d6b23)

**A 440 Hz tone generated inside the reconstructed engine reached the speakers
through the complete console frame path and was CONFIRMED BY EAR** (user,
2026-08-28; logged peak 0.999 full-scale). The probe is removed per the plan.

The slices that closed the gate:
* **The Mixer voice path REAL** (322d69cc): ProcessInputPlugIns @0x82B6A048
  (chunked source pull, aux assembly, the unconditional publish + src/dst
  ping-pong) + HandleBufferStatusUnavailable @0x82B69F78 (the decay-tail
  ring-out — the producer of the System.cpp mfParam2C expel compare).
* **The stage-carve GetSize audit** (d4fd28c0): the whole plug-in family on
  host sizeof; the console ICF-folded shared getters (Delay @0x82B96A38 /
  Limiter1 @0x82B97DA8 / RawPuller2 @0x82B97348) UNFOLDED at the records.
* **The DAC voice + the probe root-cause** (035d6b23): the DAC is created as
  the console's one-stage voice (class 255, walked last); ⭐ the a4 record of
  PlugIn::CreateInstance IS the VoiceStageConfig (context +0, init byte +8) —
  the separate host 'PlugInCreateDesc' view mis-read a pointer byte as the
  channel count (≈246) and the sine scribbled the whole mixer (the one probe
  crash, AV in the chunk append; Windows fault RVA resolved via the cgsmap);
  PlugIn's explicit mpVTable member retired (the hidden host vptr IS +0x00 —
  the generic and BaseView layouts now align slot-for-slot); the Mixer block
  zeroed at create (console kernel-zeroed-page parity).

**End state:** the engine mixes silence through the FULL console path every
5.3 ms (seed → walk → the DAC voice's Process → ReOrder → Clip → the PC fold),
the "first VALID mixed frame" indicator fires at boot, zero new asserts.

**NEXT (phase E):** the five decoded callback bodies (plugin_callbacks_decode_
codex.md) → registrations 6/9/14/15/18; SndPlayer1 (sndplayer1_decode_codex.md
— codex task in flight at session end); SubMix/GainFader/LowPassButterworth;
the three custom game descriptors; then the splice staging (88 voices).
